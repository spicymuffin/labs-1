def modCount(n, m):
    """how many numbers between 1 and n are evenly divisible by m

    Args:
        n (int): the integer we have to check until
        m (int): the divisor

    Returns:
        int: the count of numbers that are divisible by m
    """
    return n//m # this works
